package omni.automation.graffle;

@:native("HorizontalAlignment")
extern class HorizontalAlignment {

    //Align to horizontal centers.
    static var Center (default,null): HorizontalAlignment;

    // Align to left edges.
    static var Left (default,null): HorizontalAlignment;

    // Align to right edges.
    static var Right (default,null): HorizontalAlignment;
}
