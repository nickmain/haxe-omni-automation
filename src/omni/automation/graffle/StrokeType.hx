package omni.automation.graffle;

@:native("StrokeType")
extern class StrokeType {

    // Double.
    static var Double (default,null): StrokeType;

    // Freehand.
    static var Freehand (default,null): StrokeType;

    // Inner.
    static var Inner (default,null): StrokeType;

    // Outer.
    static var Outer (default,null): StrokeType;

    // Plastic.
    static var Plastic (default,null): StrokeType;

    // Single.
    static var Single (default,null): StrokeType;
}
